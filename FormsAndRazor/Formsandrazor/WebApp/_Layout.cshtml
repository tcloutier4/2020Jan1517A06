<!DOCTYPE html>
<html>
<head>
    <title>@Page.Title</title>
    <script src="~/Scripts/jquery-3.4.1.js"></script>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />

    @RenderSection("head", required: false)
</head>
<body>
    <div class="container-fluid">
        @*A RenderSection is a secondary context block that can be used for specific non main content body display
        
        We will use the "banner" RenderSection to handle the page title and menu.
        
        RenderSection is required on each content page unless  the required parameter has been set to false*@
        <div class="jumbotron">
            @RenderSection("banner", required: false)
        </div>
        @*RenderBody is the main default area where your context page will be displayed. The RenderBody reference is physically removed and your content inserted*@
        @RenderBody()

    @RenderSection("footer", required: false)
    </div>
</body>
</html>
